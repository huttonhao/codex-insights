# RAG Mention Only

This project has a product note that mentions RAG and semantic search as future capabilities.

There is no architecture document, implementation source, indexing pipeline, retriever, evaluation harness, or CI coverage.
