# RAG Architecture

The proposed flow is document loader to chunking to embeddings to vector store to retriever to grounded generation with citations.

The design also describes tenant isolation, metadata filters, retrieval metrics such as recall and NDCG, and observability.
